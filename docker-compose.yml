version: '3.8'

services:
  todo-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=your-production-secret-key-here
    volumes:
      - ./todos.db:/app/todos.db
    restart: unless-stopped